# Development Dependencies
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=9.0.3
pytest-cov>=4.1.0
pytest-mock>=3.15.1
pytest-flask>=1.2.0

# Code Quality
# NOTE: black/isort/flake8 are PINNED to exact versions. These tools change their
# formatting/lint output between releases, so an unpinned ">=" caused CI to drift
# (a newer black would reformat code that already passed locally) and even install
# different versions across the Python matrix. Keep these in sync with
# .pre-commit-config.yaml. Pinned to the newest releases that still support 3.10+.
black==25.11.0
flake8==7.3.0
flake8-docstrings==1.7.0
isort==6.1.0
pylint>=3.0.0
mypy>=1.7.0

# Security
bandit[toml]>=1.7.6
safety>=3.7.0

# Pre-commit
pre-commit>=3.5.0

# Documentation
# Sphinx 7.2.6+ requires Python 3.9+, use constraint for 3.8 compatibility
sphinx>=7.1.0,<9.2.0
sphinx-rtd-theme>=3.1.0

# Utilities
ipython>=9.13.0
ipdb>=0.13.13
