# Python dependencies for Receipt Manager (macOS)
# Installed in venv to avoid externally-managed-environment errors

pdf2image>=1.16.0
easyocr>=1.7.0
Pillow>=10.0.0
opencv-python>=4.8.0
torch>=2.0.0
torchvision>=0.15.0
